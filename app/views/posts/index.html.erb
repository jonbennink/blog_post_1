<p id="notice"><%= notice %></p>


<table>
  <!-- <thead>
    <tr>
      <th id='title'>Title</th>
    <tr>
      <th id='author'>Author</th>
    <tr>
      <th>Blog entry</th>
      <th colspan="4"></th>
    </tr>
  </thead> -->

  <tbody>
    <% @posts.each do |post| %>


    <link rel="stylesheet" href="/css/master.css">
    <div class="btn-group" role="group" aria-label="...">
      <tr>
        <td><center><%= post.title %></center></td>
      </tr>
      <tr>
        <td><center><%= post.author %></center></td>
      </tr>
      <tr>
        <td><center><%= post.blog_entry %></center></td>
      </tr>
        <tr>
          <td><center><button type="button" class="btn btn-default"><%= link_to 'Comment', post %></center></button></td>
        <!-- <td><%= link_to 'Comment', post %></td> -->
          </tr>
      <tr>
        <td><center><button type="button" class="btn btn-default"><%= link_to 'Edit', edit_post_path(post) %></center></button></td>
      </tr>
      <tr>
        <td><center><button type="button" class="btn btn-default"><%= link_to 'Delete', post, method: :delete, data: { confirm: 'Are you sure?' } %></center></button></td>
      </tr>
      </tr>
        <td>  <%=Time.current.in_time_zone("GMT") %> <td>
      </div>

    <% end %>
  </tbody>
</table>


<br>

<%= link_to 'New Post', new_post_path %>
